/**
 * @description : Given Integers a, b, and c, return true if a and b add up to c
 * @author      : vetealaurent
**/
public with sharing class SumEquals {

    private Boolean sumEquals(Integer a, Integer b, Integer c) {
        return a + b == c;
    }
}